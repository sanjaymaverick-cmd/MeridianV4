# Runtime (decision engine + research baseline)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4

# Paper quotes + BSE/crypto/forex backfill
yfinance>=0.2.40
# NSE historical (stocks, index, F&O, RBI current rates)
jugaad-data>=0.29

# Tests
pytest>=8.0

# OpenAlgo SDK — install on the box that talks to the host
# openalgo>=1.0

# Offline trainer (live path still JSON logistic if booster missing)
lightgbm>=4.0
# optuna>=3.0
# mlfinlab  # if licensed; patterns are already inlined in src/meta_label/
